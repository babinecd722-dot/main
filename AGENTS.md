# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently an empty/placeholder repo. It contains no application code, no services, no dependencies, and no build/test/lint configuration.

- **No package manager or lockfile** is present (`package.json`, `requirements.txt`, `go.mod`, etc.).
- **No setup scripts** exist (`Makefile`, `setup.sh`, `docker-compose.yml`, etc.).
- **No source code** to build, lint, or test.
- **No services** to start or connect to.

When application code is added in the future, this section should be updated with:
- How to install dependencies and which package manager to use.
- How to run the dev server, tests, and linter.
- Any non-obvious environment setup caveats.
